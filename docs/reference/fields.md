<!-- Code generated by entlite. DO NOT EDIT. -->

# Fields

Holds the field builders used to describe an entity schema.

## Matrix

Which option each field type accepts.

| Field | Unique | Default | DefaultFunc | ProtoField | Contracts | Immutable | Optional | Validate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `field.String` | yes | yes | yes | yes | yes | yes | yes | yes |
| `field.Bool` | – | yes | – | yes | yes | – | – | yes |
| `field.Int` | – | yes | – | yes | yes | – | yes | yes |
| `field.Int64` | – | yes | – | yes | yes | – | yes | yes |
| `field.Float` | – | yes | – | yes | yes | – | yes | yes |
| `field.Time` | – | yes | yes | yes | yes | yes | yes | yes |
| `field.Byte` | yes | – | yes | yes | yes | yes | yes | yes |
| `field.JSON` | – | yes | yes | yes | yes | yes | yes | yes |

## String

`field.String(name string) StringFieldBuilder`

Creates a string field with the given column name.

| Method | Description |
| --- | --- |
| `Unique() StringFieldBuilder` | Makes the value unique across all rows. |
| `Default(string) StringFieldBuilder` | Sets a fixed default value, it clears DefaultFunc. |
| `DefaultFunc(func() string) StringFieldBuilder` | Sets a default value computed on insert, it clears Default. |
| `ProtoField(int) StringFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) StringFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Immutable() StringFieldBuilder` | Blocks changes after the row is created. |
| `Optional() StringFieldBuilder` | Allows the column to be NULL. |
| `Validate(func(string) bool) StringFieldBuilder` | Checks the value before it is written. |

## Bool

`field.Bool(name string) BoolFieldBuilder`

Creates a bool field with the given column name.

| Method | Description |
| --- | --- |
| `Default(bool) BoolFieldBuilder` | Sets a fixed default value. |
| `ProtoField(int) BoolFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) BoolFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Validate(func(bool) bool) BoolFieldBuilder` | Checks the value before it is written. |

## Int

`field.Int(name string) IntFieldBuilder`

Creates an int32 field with the given column name.

| Method | Description |
| --- | --- |
| `Default(int32) IntFieldBuilder` | Sets a fixed default value. |
| `ProtoField(int) IntFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) IntFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Optional() IntFieldBuilder` | Allows the column to be NULL. |
| `Validate(func(int32) bool) IntFieldBuilder` | Checks the value before it is written. |

## Int64

`field.Int64(name string) Int64FieldBuilder`

Creates an int64 field with the given column name.

| Method | Description |
| --- | --- |
| `Default(int64) Int64FieldBuilder` | Sets a fixed default value. |
| `ProtoField(int) Int64FieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) Int64FieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Optional() Int64FieldBuilder` | Allows the column to be NULL. |
| `Validate(func(int64) bool) Int64FieldBuilder` | Checks the value before it is written. |

## Float

`field.Float(name string) FloatFieldBuilder`

Creates a float64 field with the given column name.

| Method | Description |
| --- | --- |
| `Default(float64) FloatFieldBuilder` | Sets a fixed default value. |
| `ProtoField(int) FloatFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) FloatFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Optional() FloatFieldBuilder` | Allows the column to be NULL. |
| `Validate(func(float64) bool) FloatFieldBuilder` | Checks the value before it is written. |

## Time

`field.Time(name string) TimeFieldBuilder`

Creates a timestamp field with the given column name.

| Method | Description |
| --- | --- |
| `Default(time.Time) TimeFieldBuilder` | Sets a fixed default value, it clears DefaultFunc. |
| `DefaultFunc(func() time.Time) TimeFieldBuilder` | Sets a default value computed on insert, it clears Default. |
| `ProtoField(int) TimeFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) TimeFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Immutable() TimeFieldBuilder` | Blocks changes after the row is created. |
| `Optional() TimeFieldBuilder` | Allows the column to be NULL. |
| `Validate(func(time.Time) bool) TimeFieldBuilder` | Checks the value before it is written. |

## Byte

`field.Byte(name string) ByteFieldBuilder`

Creates a bytes field with the given column name.

| Method | Description |
| --- | --- |
| `Unique() ByteFieldBuilder` | Makes the value unique across all rows. |
| `Optional() ByteFieldBuilder` | Allows the column to be NULL. |
| `Immutable() ByteFieldBuilder` | Blocks changes after the row is created. |
| `ProtoField(int) ByteFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) ByteFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `DefaultFunc(func() []byte) ByteFieldBuilder` | Sets a default value computed on insert. |
| `Validate(func([]byte) bool) ByteFieldBuilder` | Checks the value before it is written. |

## JSON

`field.JSON(name string) JSONFieldBuilder`

Creates a json field with the given column name.

| Method | Description |
| --- | --- |
| `Optional() JSONFieldBuilder` | Allows the column to be NULL. |
| `Immutable() JSONFieldBuilder` | Blocks changes after the row is created. |
| `ProtoField(int) JSONFieldBuilder` | Pins the proto field number, so it stays stable. |
| `Contracts(contracts ...entlite.Contract) JSONFieldBuilder` | Limits the field to the given layers, sqlc or proto. |
| `Default(string) JSONFieldBuilder` | Takes raw json text, e.g. `{}` or `{"theme":"dark"}` |
| `DefaultFunc(func() string) JSONFieldBuilder` | Sets a default value computed on insert, it clears Default. |
| `Validate(func(string) bool) JSONFieldBuilder` | Checks the value before it is written. |
