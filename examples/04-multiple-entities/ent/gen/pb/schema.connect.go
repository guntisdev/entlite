// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: schema.proto

package pb

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PostServiceName is the fully-qualified name of the PostService service.
	PostServiceName = "entlite.PostService"
	// UserServiceName is the fully-qualified name of the UserService service.
	UserServiceName = "entlite.UserService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PostServiceGetByIDProcedure is the fully-qualified name of the PostService's GetByID RPC.
	PostServiceGetByIDProcedure = "/entlite.PostService/GetByID"
)

// PostServiceClient is a client for the entlite.PostService service.
type PostServiceClient interface {
	GetByID(context.Context, *connect.Request[GetPostByIDRequest]) (*connect.Response[Post], error)
}

// NewPostServiceClient constructs a client for the entlite.PostService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPostServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PostServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	postServiceMethods := File_schema_proto.Services().ByName("PostService").Methods()
	return &postServiceClient{
		getByID: connect.NewClient[GetPostByIDRequest, Post](
			httpClient,
			baseURL+PostServiceGetByIDProcedure,
			connect.WithSchema(postServiceMethods.ByName("GetByID")),
			connect.WithClientOptions(opts...),
		),
	}
}

// postServiceClient implements PostServiceClient.
type postServiceClient struct {
	getByID *connect.Client[GetPostByIDRequest, Post]
}

// GetByID calls entlite.PostService.GetByID.
func (c *postServiceClient) GetByID(ctx context.Context, req *connect.Request[GetPostByIDRequest]) (*connect.Response[Post], error) {
	return c.getByID.CallUnary(ctx, req)
}

// PostServiceHandler is an implementation of the entlite.PostService service.
type PostServiceHandler interface {
	GetByID(context.Context, *connect.Request[GetPostByIDRequest]) (*connect.Response[Post], error)
}

// NewPostServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPostServiceHandler(svc PostServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	postServiceMethods := File_schema_proto.Services().ByName("PostService").Methods()
	postServiceGetByIDHandler := connect.NewUnaryHandler(
		PostServiceGetByIDProcedure,
		svc.GetByID,
		connect.WithSchema(postServiceMethods.ByName("GetByID")),
		connect.WithHandlerOptions(opts...),
	)
	return "/entlite.PostService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PostServiceGetByIDProcedure:
			postServiceGetByIDHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPostServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPostServiceHandler struct{}

func (UnimplementedPostServiceHandler) GetByID(context.Context, *connect.Request[GetPostByIDRequest]) (*connect.Response[Post], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("entlite.PostService.GetByID is not implemented"))
}

// UserServiceClient is a client for the entlite.UserService service.
type UserServiceClient interface {
}

// NewUserServiceClient constructs a client for the entlite.UserService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewUserServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) UserServiceClient {
	return &userServiceClient{}
}

// userServiceClient implements UserServiceClient.
type userServiceClient struct {
}

// UserServiceHandler is an implementation of the entlite.UserService service.
type UserServiceHandler interface {
}

// NewUserServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewUserServiceHandler(svc UserServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	return "/entlite.UserService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedUserServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedUserServiceHandler struct{}
